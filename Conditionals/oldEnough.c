#include <stdio.h>

int main(void) {
    int age;
    printf("what is your name?:\n");
    scanf("%d",age);
    if (age >=18){
        printf("you are old enough to vote!");
    }else if (age >= 16){
        printf("your can drive!");
    }else if (age >= 15){
        
    }

    return 0;
}